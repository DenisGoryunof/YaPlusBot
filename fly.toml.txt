app = "your-bot-name"  # Замените на уникальное имя

[build]
  dockerfile = "Dockerfile"

[env]
  # Переменные окружения будут добавлены позже через CLI

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

[mounts]
  source = "data_volume"
  destination = "/persistent"